name: demo-action

on:
  # push:
  #   branches: main

    issues:
      types: opened

    workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run a sample
        run: echo 'Hello World'
      
